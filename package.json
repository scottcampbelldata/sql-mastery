{
  "name": "sql-mastery-runner",
  "version": "1.0.0",
  "private": true,
  "description": "Local PostgreSQL runner for the SQL Mastery Path curriculum.",
  "main": "dist/server.js",
  "scripts": {
    "start": "node dist/server.js",
    "dev": "npm --prefix client run dev",
    "typecheck": "tsc --noEmit -p tsconfig.json",
    "build:server": "tsc -p tsconfig.json",
    "extract-lessons": "tsc -p tsconfig.json && node dist/scripts/extract-lessons.js",
    "build": "tsc -p tsconfig.json && node dist/scripts/extract-lessons.js && npm --prefix client run build",
    "test": "tsc -p tsconfig.json && node --test dist/test/*.test.js && npm --prefix client test"
  },
  "dependencies": {
    "dotenv": "^17.2.3",
    "express": "^5.2.1",
    "pg": "^8.16.3"
  },
  "engines": {
    "node": ">=18"
  },
  "devDependencies": {
    "@types/express": "^5.0.6",
    "@types/node": "^26.1.0",
    "@types/pg": "^8.20.0",
    "typescript": "^6.0.3"
  }
}
